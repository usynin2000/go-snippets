# go-snippets

Сниппет для тех, кто только начинает работать с языком GO

Запускать программы можно через:

```bash
go run filename
```

ИЛИ 

Если хочешь скомпилировать в исполняемый файл, используй:
```bash
go build hello_world.go
./hello_world
```


При переходе с Python на Go есть несколько ключевых моментов, которые стоит учитывать:

1. Строгая типизация
В отличие от Python, где переменные имеют динамическую типизацию, в Go все переменные имеют строгий тип. Например:
```go
var x int = 10  // Явное объявление типа
y := 20         // Неявное объявление, но тип фиксируется (int)
```


2. Компиляция vs. Интерпретация
Go — это компилируемый язык, поэтому код нужно компилировать перед запуском. Это даёт преимущество в скорости.


3. Явное управление ошибками
В Go нет исключений, как в Python (try/except). Вместо этого функции часто возвращают ошибки, которые нужно обрабатывать:
```go
package main

import (
    "errors"
    "fmt"
)

func divide(a, b float64) (float64, error) {
    if b == 0 {
        return 0, errors.New("деление на ноль")
    }
    return a / b, nil
}

func main() {
    result, err := divide(10, 0)
    if err != nil {
        fmt.Println("Ошибка:", err)
    } else {
        fmt.Println("Результат:", result)
    }
}
```


4. Отсутствие классов и ООП

В Go нет классов и наследования, но есть структуры (struct) и интерфейсы. Например:
```go
type Person struct {
    Name string
    Age  int
}

func (p Person) Greet() {
    fmt.Println("Привет, меня зовут", p.Name)
}
```


5. Горутины и конкурентность
Go поддерживает лёгкие потоки выполнения (горутины), что упрощает работу с многопоточностью:
```go
go myFunction()  // Запускает функцию в отдельной горутине
```


6. Отсутствие while, только for
В Go нет while, используется только for:
```go
for i := 0; i < 5; i++ {
    fmt.Println(i)
}
```

7.  Разница между := и = в Go


:=	Объявляет новую переменную и присваивает ей значение	(x := 10)

=	Присваивает значение уже существующей переменной	(x = 20)


# Темы, которые надо еще освоить:
1. Context (высокий приоритет) 🔥
Управление временем жизни операций
Отмена операций
Таймауты и дедлайны
Передача значений через контекст
Используется везде в HTTP, БД, горутинах
2. Time (работа со временем)
Парсинг и форматирование времени
Duration и Sleep
Ticker и Timer
Часовые пояса
3. IO операции
Работа с файлами (os.File)
bufio (буферизованный ввод/вывод)
io.Reader и io.Writer интерфейсы
ioutil (deprecated, но часто встречается)
4. Sync package (синхронизация) 🔥
Mutex и RWMutex
sync.Once
sync.Pool
sync.Cond
sync.Map
5. Encoding/Decoding
XML
CSV
Base64
Gob (бинарная сериализация Go)
6. Regexp (регулярные выражения)
Поиск и замена
Группы захвата
Валидация
7. Sort (сортировка)
Сортировка слайсов
Кастомная сортировка
sort.Interface
8. Reflect (рефлексия)
TypeOf и ValueOf
Работа с метаданными типов
Динамический вызов методов
9. Testing (расширенное тестирование)
Table-driven tests
Subtests
Benchmarks
Examples
Mock и testing/iotest
10. Database/SQL
Подключение к БД
Query и Exec
Prepared statements
Транзакции
11. Templates
text/template
html/template
12. Methods и Embedding
Разница между методами и функциями
Pointer vs value receivers
Встраивание структур (composition)
Мои рекомендации по приоритету:
Высокий приоритет (must-have):
Context - используется повсеместно в реальных проектах
Sync (Mutex, RWMutex) - критично для конкурентности
IO операции - работа с файлами базовая необходимость
Time - часто нужна в любом проекте
Средний приоритет:
Methods и Embedding - важно для понимания ООП в Go
Sort - часто используется
Regexp - полезно для обработки текста
Testing (расширенное) - для качественного кода
Низкий приоритет (можно отложить):
Reflect - сложная тема, нужна редко
Templates - нужны для веб-разработки
Database/SQL - если планируете работать с БД